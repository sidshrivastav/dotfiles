# WiFi Auto-Connect Configuration Example
# Copy this file to config.conf and update the paths for your setup

# Path to the wifi-autoconnect.sh script
# Update this to match your dotfiles location
WIFI_SCRIPT_PATH="/home/yourusername/dotfiles/wifi-autoconnect.sh"

# Path to Hyprland configuration file
# This could be the actual config or a symlinked version in your dotfiles
HYPRLAND_CONFIG_PATH="/home/yourusername/dotfiles/hyprland/hyprland.conf"

# Enable auto-start with Hyprland (true/false)
# Set to false if you don't want automatic startup integration
AUTO_START_ENABLED="true"

# Optional: Custom log file location (defaults to ~/.wifi-autoconnect.log)
# LOG_FILE_PATH="/home/yourusername/.config/wifi-autoconnect.log"

# Optional: WiFi interface name (auto-detected if not specified)
# WIFI_INTERFACE="wlan0"

# Optional: Connection timeout in seconds (defaults to 30)
# CONNECTION_TIMEOUT="30"

# Optional: Maximum retry attempts (defaults to 3)
# MAX_RETRIES="3"